// SET //
/*
1.stores unique elements in sorted order
2.dont allow duplicate elements
3.Sets are useful when you need to maintain a sorted collection of unique elements and efficiently perform insertion, deletion, and searching operations
4.do not provide direct access to elements by index

*/
#include <bits/stdc++.h>
using namespace std;



int main()
{
    set<int> se;
    se.insert(10);
    se.insert(20);
    se.insert(30);
    se.insert(40);
    
    return 0;
}